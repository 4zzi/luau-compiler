# luau-compiler
uses luau::compile and bytecode::sign_bytecode then creates a file called Compiled.txt where the exe is with the content in it

# usage
luaucompiler.exe script.lua

# how to build
run build_solution.bat then go to build x64 release output
